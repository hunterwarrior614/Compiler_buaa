[TOC]
## 优化概述

本编译器的优化主要集中在中端（MidEnd），通过 `OptimizeManager` 进行统一管理。优化过程采用了多次迭代的方式，以确保不同优化Pass之间能够相互促进，达到更好的优化效果。

主要的代码位于 `src/optimize` 目录下，实现了 `Optimizer` 抽象类，保证了接口的统一。

## SSA & Mem2Reg

为了方便进行后续的数据流分析和优化，将 LLVM IR 转换为 SSA 形式是至关重要的。`MemToReg` Pass 将栈上的局部变量（`alloca`）提升为虚拟寄存器。

### 困难点：Phi 节点的插入与变量重命名

SSA 的核心在于每个变量只被赋值一次。在存在控制流汇聚的地方，不同路径传来的值可能不同，需要插入 `Phi` 节点。如何确定在哪里插入 `Phi` 节点，以及如何正确地重命名变量，是实现的难点。

### 解决方案

采用了经典的算法：

1.  **支配边界（Dominance Frontier）**：计算每个基本块的支配边界。对于每个定义了变量 $v$ 的块，在其支配边界处插入 $v$ 的 `Phi` 节点。
2.  **变量重命名**：使用栈（Stack）来维护变量的当前版本。对支配树进行 DFS 遍历，遇到定义时压栈，遇到使用时取栈顶元素。

```java
// InsertPhi.java
private void placePhiNodes() {
    // ...
    while (!workList.isEmpty()) {
        IrBasicBlock block = workList.poll();
        for (IrBasicBlock frontier : block.getDominateFrontiers()) {
            if (!phiBlocks.contains(frontier)) {
                this.createPhi(frontier); // 在支配边界插入 Phi
                // ...
            }
        }
    }
}
```

## 死代码消除 (DCE)

死代码消除旨在移除程序中不会被执行或对程序结果没有影响的代码，从而减小代码体积，提高执行效率。

### 困难点：副作用与控制流

在删除代码时，最大的困难在于判断一条指令是否有**副作用**（Side Effect），以及删除指令后如何维护控制流图（CFG）的正确性。例如，函数调用可能修改全局变量，不能随意删除。

### 解决方案

采用了 `RemoveDeadCode` 和 `RemoveUnreachCode` 两个 Pass 协同工作。

1.  **不可达代码消除**：通过从入口块进行 DFS 遍历，标记所有可达的基本块。未被标记的块即为不可达块，可以直接移除。
2.  **无用指令消除**：基于活跃性分析和副作用分析。
    *   构建调用图（Call Graph），识别无副作用的函数。
    *   对于 `Store` 指令，如果后续没有 `Load` 使用该地址，或者被覆盖，则视为冗余存储。
    *   对于分支指令，如果 `True` 和 `False` 目标相同，则简化为 `Jump`。

```java
// RemoveDeadCode.java
private boolean eliminateRedundantStores() {
    // ...
    for (IrInstr instr : block.getInstrs()) {
        if (instr instanceof StoreInstr store) {
            IrValue addr = store.getAddress();
            if (lastStores.containsKey(addr)) {
                deadStores.add(lastStores.get(addr)); // 标记被覆盖的存储为死代码
                changed = true;
            }
            lastStores.put(addr, store);
        }
        // ...
    }
    // ...
}
```

## 全局值编号 (GVN)

全局值编号用于消除冗余计算。如果两个表达式计算出相同的值，则可以使用其中一个的结果代替另一个。

### 困难点：跨基本块的等价性判定

局部值编号（LVN）仅限于基本块内，而 GVN 需要在全局范围内识别等价表达式。难点在于如何高效地在支配树上传递值信息，并处理作用域问题（离开子树后失效）。

### 解决方案

采用了基于哈希（Hash）和支配树遍历的方法：
1.  **哈希计算**：为每条指令计算哈希值（例如 `ADD %1, %2` 的哈希由操作码和操作数决定）。
2.  **支配树遍历**：沿着支配树遍历基本块。维护一个 `ValueTable` 映射哈希值到指令。
3.  **常量折叠**：在 GVN 过程中同时进行常量折叠，进一步增加优化的机会。
4.  **作用域管理**：在递归处理完子节点后，将当前节点添加的哈希条目从 `ValueTable` 中移除，保证正确性。

```java
// GVN.java
private void run(IrBasicBlock block) {
    // ...
    String key = computeHash(instr);
    if (key != null) {
        if (valueTable.containsKey(key)) {
            // 发现冗余计算，进行替换
            instr.replaceAllUsesWith(existing);
            // ...
        } else {
            valueTable.put(key, instr);
            addedKeys.add(key);
        }
    }
    // ...
    // 离开节点时清理环境
    for (String key : addedKeys) {
        valueTable.remove(key);
    }
}
```

## Phi 指令消除

在生成目标代码（MIPS）之前，必须消除 IR 中的 `Phi` 指令，因为物理机器不支持 `Phi`。

### 困难点：并行语义与关键边

`Phi` 指令具有并行赋值的语义，直接转换为串行的 `Move` 指令可能会导致错误（例如 `a = b; b = a` 的交换操作）。此外，在关键边（Critical Edge）上插入指令可能会破坏 CFG 结构。

### 解决方案

采用了 `RemovePhi` Pass，分两步进行：
1.  **Lower to Parallel Copy**：将 `Phi` 指令转换为 `ParallelCopy` 指令。对于关键边，通过拆分边（插入中间块）来解决。
2.  **Sequentialize**：将 `ParallelCopy` 串行化为 `Move` 指令。通过拓扑排序或引入临时变量来解决循环依赖（Swap 问题）。

```java
// RemovePhi.java
private void lowerPhisToParallelCopies() {
    // ...
    if (pred.getNextBlocks().size() > 1) {
        pcopy = insertCopyOnSplitEdge(pred, block); // 处理关键边
    } else {
        pcopy = insertCopyAtEnd(pred);
    }
    // ...
}
```

## 寄存器分配

寄存器分配是将无限的虚拟寄存器映射到有限的物理寄存器的过程。

### 困难点：活跃区间分析与冲突处理

需要准确计算每个变量的活跃区间（Live Interval），以确定哪些变量在某时刻必须驻留在寄存器中。当寄存器不足时，需要进行溢出（Spill）处理（虽然本实验主要采用简单的分配策略，但也涉及此问题）。

### 解决方案

1.  **活跃性分析 (`ActiveAnalysis`)**：计算每个基本块的 `LiveIn` 和 `LiveOut` 集合，以及指令级的 Def-Use 信息。
2.  **线性扫描/图着色简化版 (`RegisterAllocator`)**：
    *   遍历支配树，模拟寄存器状态。
    *   **Last Use 分析**：如果一个变量在当前指令后不再被使用（且不是 LiveOut），则立即释放其占用的寄存器。
    *   优先分配空闲寄存器，维护 `Reg -> Value` 和 `Value -> Reg` 的映射。

```java
// RegisterAllocator.java
private void releaseRegisters(IrInstr instr, Map<IrValue, IrInstr> lastUses, Set<IrValue> killed) {
    // ...
    if (valueToReg.containsKey(op) &&
            lastUses.get(op) == instr && // 当前是最后一次使用
            !instr.getIrBasicBlock().getOutValueSet().contains(op)) {
        // 释放寄存器
        Register reg = valueToReg.get(op);
        regToValue.remove(reg);
        // ...
    }
}
```

## 窥孔优化

窥孔优化是在生成目标代码后，通过在一个很小的滑动窗口内检查指令序列，寻找并替换为更高效的指令序列。这部分代码位于 `src/backend/PeepHole.java`。

### 困难点：指令间的依赖关系

在进行指令替换或删除时，必须确保不破坏程序的语义。例如，删除一个 Store 指令前，必须确认后续没有 Load 指令依赖于该存储值，或者该存储值确实是冗余的。

### 解决方案

实现了多种窥孔优化策略，并在 `peep()` 方法中循环执行直到不动点：

1.  **连续存储消除**：如果连续两条指令向同一个地址存储数据，且中间没有跳转标签，则第一条存储是多余的。
2.  **跳转链扁平化**：将 `j L1 -> L1: j L2` 优化为 `j L2`，减少跳转次数。
3.  **冗余跳转消除**：如果跳转的目标就是下一条指令，则该跳转指令可以删除。
4.  **冗余加载消除**：如果 Load 指令加载的是刚刚 Store 进去的值（且寄存器未被修改），或者连续 Load 同一个地址，则可以复用寄存器。

```java
// PeepHole.java
public void peep() {
    boolean finished = false;
    while (!finished) {
        finished = removeContinuousStores();
        finished &= removeJumpToNextLabel();
        finished &= flattenJumpChains();
        finished &= removeLoadAfterStoreSameReg();
        finished &= removeDuplicateConsecutiveLoads();
    }
}

private boolean flattenJumpChains() {
    // ...
    for (int i = 0; i < textSegment.size(); i++) {
        // ...
        if (nextAssembly instanceof MipsJump targetJump
                && targetJump.getJumpType() == MipsJump.JumpType.J) {
            textSegment.set(i, new MipsJump(MipsJump.JumpType.J, targetJump.getTargetLabel()));
            finished = false;
        }
        // ...
    }
    return finished;
}
```

## 指令选择与伪指令优化

在 IR 到 MIPS 的转换过程中，通过选择更高效的指令序列来实现特定操作，特别是针对乘除法的优化。这部分逻辑主要在 `src/midend/llvm/instr/AluInstr.java` 中实现。

### 困难点：乘除法的代价

MIPS 的 `mult` 和 `div` 指令周期较长。对于乘除常数的情况，可以通过移位（Shift）和加减法（Add/Sub）的组合来代替，但需要权衡指令数量带来的开销。

### 解决方案

#### 乘法优化
对于 `x * C`，尝试将其分解为移位和加减法。
*   **移位优化**：如果 $C = 2^k$，直接使用 `sll`。
*   **组合优化**：如果 $C$ 可以表示为 $2^n \pm 2^m$ 等形式，比较直接乘法与“移位+加减”的指令数代价，选择代价最小的方案。

```java
// AluInstr.java
private boolean optimizeMulShift(IrValue value, IrConstInt irConstInt, Register registerValue, boolean k0) {
    // ...
    // 计算不同方案的代价 score
    // 1. N <= num (const-N 条加法)
    // 2. N > num (N-const 条减法)
    // 3. 原始乘法 (score = 5)
    
    if (Math.min(score1, score2) > score3) {
        return false; // 代价过大，不优化
    }
    
    // 生成移位和加减指令序列
    if (score1 <= score2) {
        int shift = Integer.numberOfTrailingZeros(n1);
        new MipsAlu(MipsAlu.AluType.SLL, resultRegister, registerValue, shift);
        for (int i = 0; i < diff1; i++) {
            new MipsAlu(MipsAlu.AluType.ADDU, resultRegister, resultRegister, registerValue);
        }
    }
    // ...
}
```

#### 除法优化
对于 `x / C`，使用 **Magic Number** 方法将除法转化为乘法和移位操作。
*   计算一个“魔数”（Magic Number）$M$ 和移位量 $S$，使得 $x / C \approx (x * M) >> S$。
*   这种方法避免了昂贵的 `div` 指令，虽然指令数增加，但总周期数通常减少。

```java
// AluInstr.java
private void optimizeDivConst(IrValue value, long divisor, Register valueRegister, Register resultRegister) {
    // 计算 Magic Number 和 Shift
    // ...
    BigInteger magic = high;
    
    // 生成乘法(mult) + 移位(mfhi/sra) + 调整符号 的指令序列
    new MarsLi(resultRegister, magic.intValue());
    new MipsMdu(MipsMdu.MduType.MULT, resultRegister, Register.K1);
    new MipsMdu(MipsMdu.MduType.MFHI, resultRegister);
    
    if (shift > 0) {
        new MipsAlu(MipsAlu.AluType.SRA, resultRegister, resultRegister, shift);
    }
    // ...
}
```
