[TOC]

# 参考编译器介绍

我主要参考了 `sysy-compiler` 的编译器源代码，使用 `java` 实现。

## 总体结构

该编译器主要分为前端、中端和后端三个部分，并设计了工具包辅助编译器实现，以及优化模块来优化目标代码。前端负责**词法分析**和**语法分析**，得到 `Token` 流与生成抽象语法树；中端负责**语法分析**，生成符号表并**生成中间代码** `llvm`；后端则负责**目标代码** `mips` 的生成。负责处理输入输出或调试等的**工具模块**放在了工具包 `utils` 中，而负责**优化目标代码**的模块单独放在了优化模块 `optimize` 中。

## 接口设计

* `Compiler` 是编译器的入口，实现了完整的编译操作。其分别调用了 `FrontEnd`、`MidEnd` 和 `BackEnd` 的静态方法，依次实现编译的各个流程，最后调用 `ErrorRecorder` 中的静态方法实现错误的输出。其中，`IOhandler` 工具类统一管理输入输出，而 `ErrorRecorder` 使用静态字段和静态方法实现统一管理全局的编译错误。。
* 在 `FrontEnd` 中，使用**静态方法**提供单例访问，使用静态字段 `lexer` 和 `parser` 分别作为词法分析器和语法分析器，明确区分词法分析和语法分析阶段，并调用各自内部的方法实现分析过程。`MidEnd` 中，通过 `FrontEnd.GetAstTree()` 获取 AST，使用 `SymbolManger` 管理符号表作用域，通过 `Visitor` 遍历 AST 生成 IR。
* * `TokenStream` 包装 `ArrayList<Token>` 来存储 `Token` 流
  * `CompUnit` 作为 AST 根节点，各节点继承自 `Node`
  * `SymbolTable` 通过 `SymbokManger` 在访问 AST 各节点的时候管理作用域，构建符号表 

## 文件组织

* `frontend/`、`midend/` 和 `backend/`：分别存放前端、中端和后端的代码
* `error/`：用于词法分析、语法分析和语义分析各阶段的错误处理
* `optimize/`：用于优化目标代码
* `utils/`：工具包，主要负责输入输出、调试等工具的实现

# 编译器总体设计

编译器整体采用示例代码 `sysy-compiler` 的架构，使用 `java` 实现。

## 总体结构

与 `sysy-compiler` 相似，主要分为前端、中端和后端三个部分，并设计了工具包辅助编译器实现，以及优化模块来优化目标代码。

## 接口设计

与 `sysy-compiler` 相似。

## 文件组织

与 `sysy-compiler` 相似。

# 词法分析设计

## BEFORE CODING

### 实现思路

使用**有穷自动机**的思想实现词法分析（部分）：

![image-20251024235904631](C:\Users\xzhfd\AppData\Roaming\Typora\typora-user-images\image-20251024235904631.png)

### 代码设计

`Compiler` 作为编译器入口，`FrontEnd` 集中管理前端操作的实现（静态方法），`Lexer` 内借助有穷自动机的思想实现词法分析，并在相应地方做错误处理。当分析完词法后，`Compiler` 负责将结果输出到文件。

## AFTER CODING

### 代码优化

* 通过覆写 `toString` 方法代替手动输出词法成分
* 将输入输出操作提取为工具类 `IOHandler`，将错误处理提取为工具类 `ErrorRecorder`，实现功能分离

# 语法分析设计

## BEFORE CODING

### 实现思路

使用**递归下降法**实现。通过预读的方式来判断一个语法成分推导出的不同选择式，通过 EBNF 的方式解决左递归的问题，并在生成完语法树后，重构语法树，以生成由原文法推导出的语法树。

### 代码设计

**递归下降**体现在为每一个出现在推导式左侧的语法成分设计一个**子程序**，用于解析相应的语法成分。因此，设计一个 `Node` 类作为所有语法树节点的父类，并拥有一个抽象方法 `parse`，各语法成分继承 `Node` 类并实现 `parse` 方法进行相应的分析操作。词法分析器 `Parser` 持有一个语法树根节点 `CompUnit`，通过调用其 `parse` 方法来开始语法分析。 

## AFTER CODING

### 思路改进

有些情况通过预读难以解决，本实验主要体现在区分 `Stmt→LVal'='Exp';'` 与 `Stmt→[Exp]';'`，此处可以通过回溯的方式解决。

### 代码优化

* 有些词法成分的判断在不同子程序中多次出现，因此可以提取为 `Node` 的方法，各子类可以调用父类方法，实现代码复用。同理，错误判断也可以提取为 `Node` 方法。

# 语义分析设计

## BEFORE CODING

### 实现思路

语义分析涉及符号表和中间代码的生成。通过自顶向下依次遍历语法树的每一个节点，借助栈式符号表，来生成最终的符号表。

### 代码设计

通过 `MidEnd` 集中管理中端的操作（静态方法）。

生成符号表的具体操作为：

* 设计 `Symbol` 类表示一个符号，需要保存必要的信息；设计 `SymbolTable` 类表示符号表，保存相应作用域的符号。

* 在语法树节点父类 `Node` 中添加 `visit` 方法，表示遍历 AST 过程中访问到该节点时的操作，从根节点（`CompUnit`）开始，调用 `visit` 方法开始遍历。

* 如果遇到**符号的引用**，就需要确定该标识符是否定义过，并且查询其相关信息。通过覆写父类 `visit` 方法，进行相应的检查。

  符号查找操作的具体流程：

  1. 在使用标识符时，在当前作用域符号表内查询是否有符号记录，有则返回其信息，无则执行 2
  2. 利用当前作用域符号表的 `fatherTable` 引用，访问外层作用域符号表。如果没有更外层符号表了，说明该标识符未定义，报错；否则在外层作用域符号表中重新执行 1

* 如果遇到**符号的定义**，则需要将其填入符号表（语法成分）。通过覆写父类的 `visit` 方法，实施填入符号表的操作。

  符合插入操作的具体流程：

  1. 在标识符声明时，判断该标识符名称在当前作用域下是否重复。若重复则报错，否则执行 2
  2. 将标识符名称作为键，将该定义处的相关信息作为符号信息，插入到当前符号表中

* 为了方便符号表的填入操作，设计一个 `SymbolManager` 类，维护生成符合表所需的各个变量信息（如当前作用域序号，当前符号表，根符号表等），并通过调用其静态方法，实现符号表的创建、填充和插入等操作。

  `SymbolManager` 需要满足的功能：

  * 记录当前符号表、根符号表和当前符号表序号

  * 能够创建子符号表，并建立层次关系，更新当前符号表及其序号
  * 当当前符号表建立完毕，能够返回上一层符号表

## AFTER CODING

### 思路改进

* **Visitor 模式重构**：最初设计中，`visit` 方法是 `Node` 类的一部分。但在实际编码中，为了更好地分离关注点（AST 结构 vs 语义/生成逻辑），采用了 **Visitor 模式**。创建了 `VisitorCompUnit`, `VisitorStmt`, `VisitorExp` 等类，专门负责遍历 AST 并执行语义检查和 IR 生成。
* **错误处理与 IR 生成并行**：语义分析（错误检查）与中间代码生成在同一遍 AST 遍历中完成。如果发现错误，则停止 IR 生成或标记错误，确保编译器能够正确报告语义错误。

### 代码优化

* **SymbolManager 增强**：`SymbolManager` 不仅管理符号表，还维护了当前函数的上下文（如 `currentFuncType`, `hasReturn` 等），用于检查返回值类型匹配和缺少返回语句等错误。
* **符号与 IR 关联**：在 `Symbol` 类中增加了 `IrValue` 字段。当遍历到变量声明时，创建对应的 `IrValue`（如 `IrAlloca`）并存储在 `Symbol` 中；当遍历到变量使用时，从 `Symbol` 中取出 `IrValue`，实现了符号表到中间代码的桥梁。

# 代码生成设计（LLVM IR）

## BEFORE CODING

### 实现思路

我使用 LLVM IR 作为中间代码。要实现从 AST 生成 LLVM IR 并方便后续的优化，要设计良好的**数据结构**来存放 LLVM IR 的各结构成分。此外，在 LLVM IR 生成的过程中，需要有一个全局的记录与操作类 `IrBuilder` 来进行辅助。由于各成分需要对 AST 进行分析得到，所以还需针对每个语法成分设计相应的 `visit` 方法。

综上，可以得出 LLVM IR 生成的**大致流程**：通过 `Visitor` 自顶向下访问 AST，在遍历 AST 各节点的过程中 `IrBuilder` 负责记录和生成 LLVM IR 各成分的数据结构的对象，最终得到一个富有层次的 `IrModule`。

### 代码设计

* **IR 类层次结构**：
    * `IrModule`：整个编译单元，包含全局变量列表和函数列表。
    * `IrFunc`：函数定义，包含基本块列表和参数列表。
    * `IrBasicBlock`：基本块，包含指令列表，维护前驱和后继关系。
    * `IrInstr`：指令基类，派生出 `BinaryInstr` (加减乘除等), `LoadInstr`, `StoreInstr`, `BrInstr`, `CallInstr` 等。
    * `IrValue`：所有“值”的基类，实现了 **Use-Def 链**，方便后续优化。
* **辅助类**：
    * `IrBuilder`：维护当前正在构建的基本块（Current Block），提供 `createAdd`, `createBr` 等便捷方法，自动将新指令插入当前块的末尾。

## AFTER CODING

### 思路改进

* **短路求值实现**：对于 `&&` 和 `||` 逻辑表达式，不直接生成逻辑运算指令，而是通过控制流（`BrInstr`）来实现短路求值。例如 `A && B` 被转换为 `if (A) { if (B) ... }` 的形式。
* **类型系统适配**：LLVM IR 是强类型的，而 SysY 语言中存在隐式转换（如 `int` 转 `bool`）。在生成 IR 时，增加了自动类型转换逻辑，例如在 `if` 条件中自动插入 `icmp ne i32 %val, 0` 将整数转换为布尔值。

# 代码生成设计 (MIPS)

## BEFORE CODING

### 实现思路

后端负责将 LLVM IR 翻译为 MIPS 汇编代码。主要流程是遍历 `IrModule` 中的每个函数，再遍历函数中的每个基本块，最后将基本块中的每条 IR 指令翻译成对应的 MIPS 指令序列。

### 代码设计

* **MipsModule**：管理 `.text` 和 `.data` 段。
* **MipsBuilder**：辅助构建 MIPS 指令。
* **翻译策略**：
    * **栈式分配**：最简单的策略是将所有局部变量和临时变量都存放在栈上。每条指令执行前从栈加载操作数，执行后将结果存回栈。
    * **指令映射**：建立 IR 指令到 MIPS 指令的映射，例如 `add` -> `addu`, `icmp` -> `slt` + `beq` 等。

## AFTER CODING

### 思路改进

* **栈帧布局设计**：设计了标准的栈帧布局，包括参数区（Arguments）、返回地址（RA）、保存的寄存器（Saved Regs）和局部变量区（Local Vars）。
* **指令翻译完善**：完善了对各种 IR 指令的翻译逻辑，确保了生成的 MIPS 代码的正确性。例如，正确处理了函数调用时的参数传递和栈帧切换。
* **伪指令处理**：为了简化开发，部分指令使用了 Mars 模拟器支持的伪指令（如 `li`, `la`），由汇编器负责展开。

# 代码优化设计

## BEFORE CODING

### 实现思路

编译器优化的目标是提高生成代码的执行效率（减少指令数、减少访存）。计划在中间代码（IR）层面上进行大部分优化，因为 IR 结构清晰、独立于机器。

### 代码设计

* **OptimizeManager**：作为优化的入口，按顺序调度各个优化 Pass。
* **Optimizer**：抽象基类，所有优化 Pass 继承此类并实现 `optimize()` 方法。
* **计划实现的 Pass**：
    * `MemToReg`：将栈上分配的变量提升到寄存器（构建 SSA）。
    * `DCE` (Dead Code Elimination)：死代码消除。

## AFTER CODING

### 思路改进与新增优化

在开发过程中，为了追求更高的性能，实现了一系列高级优化：

1.  **SSA 形式构建 (MemToReg)**：
    *   实现了支配树（Dominator Tree）的构建。
    *   计算支配边界（Dominance Frontier）。
    *   在支配边界处插入 `Phi` 函数，并重命名变量，将代码转换为 SSA 形式。这极大地简化了后续的数据流分析。

2.  **全局值编号 (GVN)**：
    *   在 SSA 形式上实现了 GVN。通过哈希表记录已经计算过的表达式，如果再次遇到相同的表达式且操作数未变，则直接复用之前的结果，消除了公共子表达式。

3.  **全局代码移动 (GCM)**：
    *   利用支配树和循环嵌套信息，将指令移动到其执行频率最低的合法位置（例如将循环不变计算移出循环）。

4.  **控制流图简化 (SimplifyCFG)**：
    *   **死代码消除**：删除不可达的基本块。
    *   **基本块合并**：如果基本块 A 无条件跳转到 B，且 B 只有 A 一个前驱，则将 A 和 B 合并。
    *   **分支优化**：将条件为常数的分支转化为无条件跳转。

5.  **图着色寄存器分配**：
    *   实现了完整的图着色算法。
    *   **活跃变量分析**：计算每个变量的活跃区间。
    *   **冲突图构建**：根据活跃区间构建干扰图。
    *   **Coalescing (合并)**：实现了 George 和 Briggs 策略，消除冗余的 `move` 指令。
    *   **Spill (溢出)**：当寄存器不足时，根据启发式算法（如引用计数、循环深度）选择变量溢出到栈上。

6.  **窥孔优化 (Peephole)**：
    *   在后端生成 MIPS 代码后，进行最后一遍扫描。
    *   **冗余存取消除**：删除紧跟在 `store` 后的相同地址的 `load`。
    *   **跳转优化**：删除跳转到下一条指令的 `j` 指令；简化跳转链（`j L1` -> `L1: j L2` 优化为 `j L2`）。
    *   **基本块布局**：通过基本块合并和重排，减少无条件跳转指令的数量。

### 代码优化

* **Pass 调度**：发现优化 Pass 之间存在相互促进的关系（例如 DCE 可能暴露更多的 CFG 简化机会），因此在 `OptimizeManager` 中采用了循环执行策略，多次迭代执行 `GVN`, `DCE`, `SimplifyCFG` 等 Pass，直到代码收敛。





